sudo: required

os:
  - linux

addons:
  apt:
    packages:
      - docker-ce

git:
  depth: 4

services:
  - docker

script:
  - docker version
  - docker build --no-cache --tag lacledeslan/gamesvr-cssource --build-arg BUILDNODE=TravisCI ./linux
  - docker images
  - docker run lacledeslan/gamesvr-cssource ./ll-tests/gamesvr-cssource

after_script:
  - docker images
